# 2026-04-22 工作日志

## 项目修复与启动

- **BookSystem_Over 项目**：图书管理系统（Spring Boot 3.2 + Vue 3 + Element Plus）
- **后端**：Spring Boot 3.2.0，Java 17，MyBatis-Plus 3.5.7，MySQL 8.0，端口 8080
  - 数据库：citylibrary（MySQL 本地 127.0.0.1:3306），用户 root/admin123
  - 编译通过，`mvn spring-boot:run` 启动成功
  - API 验证：`/api/books` 返回 200
- **前端**：Vue 3 + Vite 4.5 + Element Plus + Pinia + ECharts + vue-router + axios
  - **修复1**：vite.config.ts 代理目标从 `http://10.5.150.205:8080` 改为 `http://localhost:8080`
  - **修复2**：项目缺少核心入口文件（index.html, main.ts, App.vue）— 已创建
  - **修复3**：缺少 i18n/index.ts 国际化配置 — 已创建
  - **修复4**：缺少 notificationStore.ts — 已创建
  - **修复5**：缺少 AdminLayout.vue — 已创建
  - **修复6**：缺少多个 views（HomeView, BookReviewView, NotificationView, UserHistoryView, UserFinesView, NotFoundView, AdminBookAddView, AdminBookImportView, AdminAcquisitionView, AdminCategoryView）— 已创建
  - **修复7**：utils/request.ts baseURL 从硬编码 `http://127.0.0.1:8080/api` 改为 `/api`
  - **修复8**：缺少 vue-i18n 依赖 — 已安装 vue-i18n@9
  - **修复9**：缺少 tsconfig.json, tsconfig.node.json, env.d.ts — 已创建
  - 当前运行在 `http://localhost:8082`（8081 端口曾占用，重启后分配了 8082）
- **修复10**：i18n 中文无法显示 — 根因是 `src/i18n/locales/zh-CN.ts`（旧版简化）和 `zh-CN/index.ts`（完整版）同时存在，Vite 模块解析优先匹配 `zh-CN.ts`，导致翻译数据不完整，`t()` 返回 key 而非翻译值。删除 `zh-CN.ts` 后问题解决
- **联调验证**：前后端均正常运行，中文 i18n 翻译正常显示
